# pygame gotchas

This file captures hard-won pitfalls. It starts empty; add entries as you
discover them. Each entry: symptom, cause, fix.

---

(Add entries below as you encounter them. The knowledge base will also
accumulate test-failure → test-fix pairs automatically.)
